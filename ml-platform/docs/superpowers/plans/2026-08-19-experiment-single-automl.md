# 实验与 AutoML 任务一对一实施计划

1. 为后端 API 和前端交互添加失败回归测试，确认现有实现允许实验复用且列表仍显示任务名。
2. 新增 `ExperimentAutoMLBinding` ORM 模型和线性 Alembic 迁移，回填历史 AutoML 任务并更新迁移 head 契约。
3. 在 AutoML 创建事务中写入绑定，以数据库唯一约束处理并发，并返回稳定 409 错误。
4. 扩展实验响应的占用字段和 AutoML 任务的实验名称字段。
5. 调整前端实验选择器和建模任务列表，确保仅选择未使用实验并显示实验名称。
6. 运行聚焦后端测试、前端测试与构建、迁移契约、编译和 `git diff --check`。
7. 将实现、验证和剩余风险追加到 `DEVELOPMENT_PLAN.md` 与共享开发经验。
