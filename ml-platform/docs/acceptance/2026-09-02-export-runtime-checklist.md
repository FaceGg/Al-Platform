# 导出与离线运行时清单

> 状态：`in_progress`。本清单定义 Task 11 与 Task 14 的验收条件，未替代实际收据。

- 导出仅允许完成、签名和校验和验证通过的模型候选。
- 包含模型、预处理、输入输出合同、映射、推理代码、依赖锁定、SBOM、签名和全量 SHA-256。
- 自动标注关联修订存在时，包含聚类、规则和策略工件；不得包含源数据、样本、标签、评论、账号、凭据或私钥。
- `predict` 与 `annotate` 在读取 CSV、Excel、Parquet、JSON、XML 前验证对应输入合同和解析合同。
- 任一列、类型、缺失策略、范围、映射、样本标识、有限数值或输出检查失败时，只保留脱敏验证报告，不发布部分结果。
- 验收必须覆盖有效输入、无效输入、包结构、签名、SBOM、校验和和一次性下载授权，并由 `EXP-01` 与 `INF-01` 的当前 SHA 收据证明。

## 2026-09-09 当前 SHA 状态

当前 SHA `e94862af844ea95a31203423c24a8ececd7553d6` 尚无 `EXP-01` 或 `INF-01` 的完整通过收据。本轮只确认了相关实现和局部聚焦测试存在；导出包内容、签名、SBOM、checksum、有效与无效离线输入、一次性下载授权以及真实运行环境验证仍待执行，不能把构建或迁移检查结果替代为导出/推理验收。

## 2026-09-09 进度整理补充

本轮前端完整 Vitest 和后端 worker 回归通过，但它们不产生 `EXP-01` 或 `INF-01` 收据。导出包结构/签名/SBOM/checksum、离线有效与无效输入、一次性下载授权及目标运行环境验证仍未执行，清单继续保持 `in_progress`。

## 2026-09-09 进度整理更正

本次只整理已有证据：前端完整 Vitest 和 worker 导入回归保持通过，但没有新增导出或离线运行时收据。`EXP-01`、`INF-01` 仍需在当前 SHA 上验证包结构、签名、SBOM、checksum、有效/无效输入、一次性下载授权和目标运行环境；清单继续保持 `in_progress`。

## 2026-09-09 聚合复核（最新）

Week 17 聚合 **21/21 模块通过**以及运行器和标签 schema API 的聚焦回归，不产生 `EXP-01` 或 `INF-01` 收据。当前 SHA 仍未完成导出包结构、签名、SBOM、checksum、有效/无效离线输入、一次性下载授权和目标运行环境验证；本清单继续保持 `in_progress`。

## 2026-09-09 当前工作树进度整理

本轮发现的前端通用任务列表回归不改变导出或离线实现范围，也没有新增 `EXP-01`/`INF-01` 收据。此前的局部测试和构建记录继续按原范围保留；导出包、签名、SBOM、checksum、有效/无效输入、一次性下载授权及目标运行环境仍需在回归修复后于同一 SHA 上验证。

## 2026-09-09 当前工作树复核更正

前端回归修复后的页面、台账、完整 Vitest 和构建验证均未执行导出包或离线运行时命令，且工作树仍有未提交变更。因此本轮不新增 `EXP-01` 或 `INF-01` 的 SHA 绑定收据；包结构、签名、SBOM、checksum、有效/无效输入、一次性下载授权及目标运行环境验证继续保持 `in_progress`。

## 2026-09-09 刷新合同复核边界

本轮新增的 Task 5 列表刷新合同验证为后端状态与前端操作链路证据，不执行导出包构建、签名、SBOM、checksum 或离线 `predict`/`annotate`。因此 `EXP-01` 与 `INF-01` 没有新增收据，仍必须在干净提交的同一 SHA 上完成全部导出和运行时验证。
